<span class="docs-mr-4">
  <input 
    type="radio"
    id={{concat name "_" option.id}}
    name={{name}}
    checked={{eq selectedOption option.id}}
    onchange={{action this.handleUpdate option}}
  >
  <label for="{{name}}_{{option.id}}">
    {{option.label}}
  </label>
</span>